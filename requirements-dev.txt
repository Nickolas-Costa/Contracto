-r requirements.txt

# Usado apenas pelos testes automatizados, para gerar PDFs modelo sintéticos
# (a aplicação em si não depende de reportlab)
reportlab==5.0.0

# Build do executável (dependências transitivas resolvidas pelo PyInstaller)
pyinstaller==6.21.0
pyinstaller-hooks-contrib==2026.6

# Auditoria de vulnerabilidades (pip-audit)
pip-audit==2.10.1
