-r requirements.txt

# Usado apenas pelos testes automatizados, para gerar PDFs modelo sintéticos
# (a aplicação em si não depende de reportlab)
reportlab>=4.0.0

# Build do executável
pyinstaller>=6.10.0
